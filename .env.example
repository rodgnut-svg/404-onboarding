# 404Found Onboarding Portal - Environment Variables Template
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to version control

# =============================================================================
# SUPABASE CONFIGURATION (Required)
# =============================================================================
# Find these values in: Supabase Dashboard → Settings → API

# Project URL - Found in "Project URL" field
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Anon/Public Key - Found in "Project API keys" section → anon/public key
# This key is safe to expose in client-side code
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Service Role Key - Found in "Project API keys" section → service_role key
# ⚠️ SECURITY WARNING: This key has admin access. NEVER commit this value!
# Only use server-side. In production, store in Vercel/Netlify env vars only.
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# =============================================================================
# BOOTSTRAP CONFIGURATION (Required)
# =============================================================================
# Generate a secure random string for bootstrap authentication
# Run: openssl rand -hex 32
# This prevents unauthorized agency creation
BOOTSTRAP_SECRET=your_secure_random_string_here

# =============================================================================
# SITE CONFIGURATION (Required)
# =============================================================================
# Base URL for your application (used for auth callbacks)
# Development: http://localhost:3000
# Production: https://your-domain.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# =============================================================================
# GOHIGHLEVEL INTEGRATION (Optional)
# =============================================================================
# Only fill these if you're using GoHighLevel integration

# Booking widget URL for embedding in Step 5 of onboarding
NEXT_PUBLIC_GHL_BOOKING_WIDGET_URL=

# Webhook URL for syncing onboarding data to GoHighLevel
GHL_WEBHOOK_URL=
